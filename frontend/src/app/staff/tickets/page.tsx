import { PlaceholderPanel } from '@/components/staff/PlaceholderPanel';

// PR-CONSULT-2 — Tickets placeholder. Lands in a later PR.
export default function StaffTicketsPage() {
  return <PlaceholderPanel section="Tickets" />;
}
