import { PlaceholderPanel } from '@/components/staff/PlaceholderPanel';

// PR-CONSULT-2 — Meetings placeholder. Lands in a later PR.
export default function StaffMeetingsPage() {
  return <PlaceholderPanel section="Meetings" />;
}
