import { AgentPayablesClient } from '@/components/agent/AgentPayablesClient';

// PR-AGENT-PORTAL phase 1 — what the agent is owed and has been paid.
export default function AgentPayoutsPage() {
  return <AgentPayablesClient />;
}
