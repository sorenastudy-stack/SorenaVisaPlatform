import { ProviderHome } from '@/components/provider/ProviderHome';

// PR-PROVIDER-PORTAL slice C — the institution's one page: who we have them
// down as, what we hold for them, and the upload.
export default function ProviderPage() {
  return <ProviderHome />;
}
