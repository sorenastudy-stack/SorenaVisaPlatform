import { ProviderProgrammes } from '@/components/provider/ProviderProgrammes';

// PR-PROVIDER-PORTAL slice D — the institution's own programme catalogue.
export default function ProviderProgrammesPage() {
  return <ProviderProgrammes />;
}
