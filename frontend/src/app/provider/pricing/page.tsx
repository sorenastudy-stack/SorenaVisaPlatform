import { ProviderPricingGroups } from '@/components/provider/ProviderPricingGroups';

// PR-PROVIDER-PORTAL slice E — country groups and the rates set against them.
export default function ProviderPricingPage() {
  return <ProviderPricingGroups />;
}
