# Sorena Visa — Design System

> **این فایل مرجع الزامی طراحی است.** هر بار که Claude Code یک صفحه، کامپوننت، یا هر عنصر UI جدید می‌سازد یا ویرایش می‌کند، **باید قبل از نوشتن کد این فایل را بخواند** و بعد از نوشتن کد، خروجی را با این فایل مطابقت دهد.
>
> این فایل دو بخش دارد: بخش ۱ قوانین ثابت و قفل‌شده (از دستورالعمل پروژه) است — تغییر ندهید. بخش ۲ («تکمیل خودکار») باید توسط Claude Code در اولین اجرا، بعد از اسکن کد واقعی پروژه، تکمیل شود.

---

## بخش ۰ — دستور اجرایی برای Claude Code (اول این را بخوان)

اگر این اولین باری است که این فایل را می‌خوانی و بخش ۲ («تکمیل خودکار») هنوز پر نشده:

1. پوشه `frontend` (یا هر مسیر Next.js/React پروژه) را اسکن کن.
2. کامپوننت‌های پایه موجود را پیدا کن: دکمه، اینپوت، کارت، مودال، بج/تگ، اسپینر/لودر، تُست/نوتیفیکیشن.
3. فایل تنظیمات Tailwind (`tailwind.config.js` یا `.ts`) را بخوان — رنگ‌ها، فونت‌ها، و breakpoint های سفارشی را استخراج کن.
4. اگر کامپوننت مشترکی (مثلاً `Button.tsx`) در چند نسخه ناهماهنگ در جاهای مختلف پروژه تکرار شده، آن را در بخش «بدهی دیزاین شناسایی‌شده» یادداشت کن — تغییرش نده مگر کاربر بخواهد.
5. بخش ۲ این فایل را با نتایج واقعی پر کن و ذخیره کن.
6. از این به بعد، در هر جلسه بعدی، فقط این فایل تکمیل‌شده را بخوان و به آن پایبند باش — دیگر نیازی به اسکن مجدد کامل نیست مگر کاربر بگوید ساختار عوض شده.

---

## بخش ۱ — قوانین ثابت (قفل‌شده، طبق دستورالعمل پروژه)

### پالت رنگ

| نقش | مقدار | استفاده |
|---|---|---|
| Primary / Navy | `#1e3a5f` | هدرها، دکمه‌های اصلی، متن برند |
| Accent / Gold | `#c9a961` | تاکید، آیکون فعال، حالت انتخاب‌شده، خط جداکننده تزئینی |
| Background | `#faf8f3` | پس‌زمینه صفحه (نه سفید خالص) |
| متن اصلی | خاکستری گرم (warm gray) — نه مشکی خالص `#000000` | بدنه متن |

قانون: هیچ رنگ جدیدی بدون دلیل مستند اضافه نشود. اگر برای یک حالت (مثلاً خطا یا موفقیت) رنگ لازم است و در این جدول نیست، Claude Code باید قبل از استفاده از آن، آن را اینجا اضافه و مستند کند — نه اینکه یک رنگ دلبخواهی در کد hardcode کند.

**رنگ‌های وضعیت (Status) — باید یکبار تعریف و همه‌جا استفاده شود:**

| وضعیت | رنگ پیشنهادی | یادداشت |
|---|---|---|
| موفقیت / تأیید شد | سبز ملایم و گرم (نه سبز نئون) | هماهنگ با تن آرام کلی برند |
| هشدار / در انتظار | زرد/کهربایی گرم — نزدیک به گلد برند | تمایز کافی با Accent اصلی حفظ شود |
| خطا / رد شد | قرمز آجری، نه قرمز فریاد‌زن | پلتفرم مهاجرتی — کاربر مضطرب است؛ خطا نباید حس تهدید بدهد |
| خنثی / اطلاعاتی | خاکستری آبی ملایم | |

> ⚠️ این چهار رنگ باید در Tailwind config به‌عنوان توکن (`success`, `warning`, `error`, `info`) تعریف شوند و **هرگز** به‌صورت hex مستقیم در JSX نوشته نشوند.

### تایپوگرافی

- انگلیسی: **Inter**
- فارسی: **Vazirmatn**
- قانون: هیچ صفحه‌ای نباید فونت سوم اضافه کند.

مقیاس پیشنهادی (اگر در کد فعلی مقیاس دیگری تثبیت شده، همان را در بخش ۲ ثبت کن، این را بازنویسی نکن):

| سطح | اندازه | وزن | مصرف |
|---|---|---|---|
| H1 | ۲۸–۳۲px | Bold | عنوان صفحه |
| H2 | ۲۲–۲۴px | Bold/Semibold | عنوان بخش |
| H3 | ۱۸–۲۰px | Semibold | عنوان کارت/زیربخش |
| Body | ۱۵–۱۶px | Regular | متن اصلی |
| Caption | ۱۲–۱۳px | Regular | برچسب، توضیح کمکی، متادیتا |

### فاصله‌گذاری (Spacing Scale)

فقط از مقیاس ۴ پیکسلی استفاده شود: `4, 8, 12, 16, 24, 32, 48, 64` (px).
هیچ مقدار دلبخواهی مثل `padding: 13px` یا `margin: 22px` مجاز نیست — نزدیک‌ترین مقدار مجاز از این مقیاس انتخاب شود.

### اجزای پایه (طبق دستورالعمل قفل‌شده پروژه)

- دکمه‌ها: حداقل ارتفاع ۴۸ پیکسل (لمس‌پذیر روی موبایل)
- گردی گوشه‌ها (border-radius): ۱۲ پیکسل — روی دکمه، کارت، مودال، اینپوت
- هر صفحه دقیقاً **یک اقدام اصلی** (Primary Action) دارد که واضح‌ترین و برجسته‌ترین عنصر صفحه است. بقیه اقدامات ثانویه‌اند و بصری کم‌رنگ‌تر.
- موبایل-فرست: طراحی از عرض موبایل شروع می‌شود، نه دسکتاپ. تست حتماً روی عرض ۳۷۵px انجام شود.
- فارسی = راست‌به‌چپ (RTL) کامل، شامل آیکون‌های جهت‌دار (فلش‌ها، breadcrumb) که باید در حالت RTL آینه شوند.
- تاریخ‌ها: `DD/MM/YYYY`. پول: عدد صحیح ذخیره می‌شود (سنت)، اما نمایش همیشه با علامت واحد پول و جداکننده هزارگان.

### زبان و لحن (Microcopy)

- انسانی و آرام، نه فنی و اداری.
  - بگو: «داریم مدارکت رو بررسی می‌کنیم»
  - نگو: «STATUS: PENDING_REVIEW»
- پیام خطا هرگز کاربر را مقصر یا نگران نکند. توضیح بده چه اتفاقی افتاده و چه کاری باید انجام شود.
- هیچ صفحه‌ای نباید کاربر را در ابهام رها کند — همیشه باید روشن باشد «الان چه اتفاقی می‌افتد» و «قدم بعدی چیست».

---

## بخش ۲ — تکمیل خودکار (Claude Code باید این را در اولین اجرا پر کند)

> این بخش عمداً خالی/قالب‌بندی‌شده رها شده. لطفاً پر کن و این هشدار را حذف کن.

### نقشه کامپوننت‌های موجود

| کامپوننت | مسیر فایل | نکته |
|---|---|---|
| Button | `[پر شود]` | [نسخه‌های واریانت: primary/secondary/danger/...] |
| Card | `[پر شود]` | |
| Input / Form field | `[پر شود]` | |
| Modal / Dialog | `[پر شود]` | |
| Badge / Tag | `[پر شود]` | استفاده برای وضعیت (Pending/Verified/Rejected) |
| Toast / Notification | `[پر شود]` | |
| Loading / Spinner | `[پر شود]` | |
| Empty state | `[پر شود]` | آیا الگوی یکسانی برای «هنوز داده‌ای نیست» وجود دارد؟ |

### توکن‌های واقعی Tailwind (از `tailwind.config`)

```
[Claude Code: خروجی واقعی رنگ‌ها/فونت‌ها/breakpoint های تعریف‌شده در tailwind.config را اینجا کپی کن]
```

### بدهی دیزاین شناسایی‌شده (Design Debt)

فهرست ناهماهنگی‌های موجود بین صفحات (مثلاً: سه‌جا دکمه اصلی رنگ متفاوت دارد، یا فاصله‌گذاری کارت‌ها در دو صفحه فرق دارد). **این‌ها را خودت اصلاح نکن** — فقط لیست کن، تا کاربر تصمیم بگیرد کِی وقت رفعشان برسد.

- `[پر شود در اسکن اول]`

### صفحات/فلوهای دارای الگوی تثبیت‌شده (به‌عنوان مرجع کپی برای صفحات جدید)

اگر یک صفحه قبلاً به‌خوبی طراحی شده (مثلاً فرم پرداخت یا کارت وضعیت پرونده)، اسمش را اینجا بیاور تا صفحات جدید مشابه از همان الگو کپی کنند، نه اینکه هر بار از صفر طراحی شوند.

- `[پر شود]`
